# Dependencies
node_modules/

# Build output
dist/

# Local data & auth
store/
data/
logs/

# Groups - only track base structure and specific CLAUDE.md files
groups/*
!groups/main/
!groups/global/
groups/main/*
groups/global/*
!groups/main/CLAUDE.md
!groups/global/CLAUDE.md

# Secrets
*.keys.json
.env
# Backups/variants (.env.bak-*, .env.local, ...) — same secrets, previously committable
.env.*
!.env.example

# Screaming Frog configs (contain API keys)
configs/*.seospiderconfig

# OS
.DS_Store

# IDE
.idea/
.vscode/

agents-sdk-docs

# Windows ADS junk + editor backups
*Zone.Identifier
*.bak

# Scratch — never committed; durable working docs go in docs/plans/
tmp/

# Client/generated artifacts and debris — never commit
audits/
reports/
playwright-audit/
supabase/.temp/

# Python bytecode
__pycache__/
*.pyc
